type MessageProps = {
  name: string;
  age: number;
  authState: boolean;
  checks: string[];
};

const Message = ({ name, age, authState, checks }: MessageProps) => {
  return (
    <>
      
    </>
  );
};

export default Message;
